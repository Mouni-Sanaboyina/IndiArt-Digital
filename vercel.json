{
  "buildCommand": "cd indiart-frontend && npm install && npm run build",
  "outputDirectory": "indiart-frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
} 